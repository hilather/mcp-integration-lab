# Copy to .env and adjust. The profile carries ports/mode/storage; this file
# only selects which profile is active (and can override any profile value,
# since .env wins over profile.env).

# Active profile: a directory under profiles/.
PROFILE=default

# Example override (uncomment to take precedence over the profile):
# LABDNS_DNS_PORT=53
