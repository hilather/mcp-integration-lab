# Guides

- [Quick start](quickstart.md) — clone, `make up`, attach a client, hit a data plane, reload one app
- [Configure](configuration.md) — every profile knob, reload vs `make up`, working YAML from `profiles/default`

Published as HTML on [the project site](https://hilather.github.io/mcp-integration-lab/).
The site is the static tree under `docs/`. Enable it once with
**Settings → Pages → Deploy from a branch → `main` / `docs`**.
